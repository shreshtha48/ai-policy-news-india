requests>=2.28
beautifulsoup4>=4.11
openpyxl>=3.1
numpy>=1.24
# Semantic layer: prototype classifier, meaning-based dedupe, and the trained
# classifier's encoder. Pulls in PyTorch; downloads a ~90 MB model on first run.
# Without it the pipeline falls back to keyword rules (run_summary.json "classifier").
sentence-transformers>=2.7
scikit-learn>=1.3        # trained classifier (logistic regression head)
joblib>=1.3
# Reading near-miss articles and decoding Google News links.
trafilatura>=1.8
googlenewsdecoder>=0.1.7
